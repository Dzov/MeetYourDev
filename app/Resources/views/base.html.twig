<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>{% block title %}Meet Your Dev{% endblock %}</title>

  {% block stylesheets %}
  <link href="{{ asset('bundles/mydplatform/css/master.css') }}" rel="stylesheet">
  {% endblock %}
</head>

<body>
  <header class="header flex">
    <a href="/platform" class="header__brand">
      <img src="">
      <h1>Meet Your Dev <br> <span></span></h1>
    </a>
    <nav class="header__nav selfCenter">
      <ul class="flex justifyBetween">
        {% if is_granted('ROLE_USER') %}
        <li><a class="" href="/profile">{{ app.user.username | capitalize }}</a></li>
        <li><a class="" href="/logout">Déconnexion</a></li>
        {% else %}
        <li><a class="" href="/register">Inscription</a></li>
        <li><a class="" href="/login">Connexion</a></li>
        {% endif %}

        {% if is_granted('ROLE_ADMIN') %}
        <li><a class="" href="/admin">Administration</a></li>
        {% endif %}
      </ul>
    </nav>
  </header>

  
  {% block body %}
  <main>
    <section class="mainSection welcome__section">
      <h1 class="welcome__title">Bienvenue ! </h1>
      <p class="welcome__intro">Inscrivez-vous pour poster une annonce ou proposer vos services de développeurs en devenir</p>
    </section>
  </main>
  {% endblock %}
  

  <hr>

  <footer>
    <p>The sky's the limit © {{ 'now'|date('Y') }} and beyond.</p>
  </footer>

  {% block javascripts %}
  {% endblock %}

</body>
</html>